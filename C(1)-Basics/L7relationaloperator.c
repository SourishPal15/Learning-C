// In this lesson we would learn about relational operators

#include <stdio.h>

int main()
{
    printf("Read the comments of this lesson");

    return 0;
}

/*

Relational operators are used to check conditions whether they are true or not.
In C, the relational operators are: 

1) == [equals to]
2) >= [greater than or equals to]
3) <= [lesser than or equals to]
4) != [not equals to]
5) > [greater than]
6) < [lesser than]

NOTE: "=" is an assignment operator which is used to assign values to variables,
whereas "==" is a relational operator which checks whether two given values in the 
conditon are equal or not, if equal then conditon is true otherwise false 

*/